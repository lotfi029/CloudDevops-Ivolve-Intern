apiVersion: apps/v1
kind: Deployment
metadata:
  name: ivolve-shared-lib-app
  namespace: ivolve
spec:
  replicas: 2
  selector:
    matchLabels:
      app: ivolve-shared-lib-app
  template:
    metadata:
      labels:
        app: ivolve-shared-lib-app
    spec:
      containers:
        - name: ivolve-shared-lib-app
          image: YOUR_DOCKERHUB_USERNAME/ivolve-app:latest
          ports:
            - containerPort: 8080
---
apiVersion: v1
kind: Service
metadata:
  name: ivolve-shared-lib-app-service
  namespace: ivolve
spec:
  type: ClusterIP
  selector:
    app: ivolve-shared-lib-app
  ports:
    - port: 8080
      targetPort: 8080
