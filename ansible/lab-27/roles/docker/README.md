# Docker Role

This role installs Docker CE on Ubuntu/Debian managed nodes.

## Tasks

- Install apt prerequisites.
- Add the Docker GPG key.
- Add the Docker apt repository.
- Install `docker-ce`.
- Start and enable the Docker service.
