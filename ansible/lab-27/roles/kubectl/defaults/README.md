# kubectl Role Defaults

`main.yml` defines the default kubectl version used by the role.

Override `kubectl_version` from inventory, group vars, host vars, or the command line when you need a different client version.
